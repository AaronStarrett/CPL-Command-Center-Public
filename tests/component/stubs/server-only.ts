// Next.js resolves this marker during application builds. Component tests use an inert alias so
// Vite can load server route modules without weakening the production server-only boundary.
export {};
