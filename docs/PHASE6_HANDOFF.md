# Phase 6A–6B handoff

**IMPLEMENTED; full local acceptance is NOT COMPLETE at this September 24, 2026 verified repair checkpoint.** Actual two-company browser inquiry/review/automatic-draft paths were observed. All 52 originally unfinished PostgreSQL session cases and two separate offline contracts passed. Three cold starts and two normal restarts passed the same ten-check fresh-session readback; expected old-cookie rejection and held-work recovery also passed. Seven installed-package typechecks and all 14 local production build tasks passed with zero cached tasks. Final visible browser reopening remains BLOCKED pending the owner’s manual reopen. This document records that checkpoint; measured browser closure, normal gates, exact commits and publication belong to the separate authoritative private stabilization release ledger. Phase 6C, production hosting and real Gmail remain DEFERRED.

## Phase 5 checkpoint

The exact accepted Phase 5 source was committed locally through the normal 18-stage pre-commit gate, with no skipped hook:

- Canonical checkpoint: `864c0b0f022b642d08476b9546829b1e2db0dd75`, on `codex/cpl-productization-phase-1`.
- Previous canonical reference: `7634a3d0503e21a8aee8a1ae0687023e9eeddffe`; no history was rewritten or reset.
- All 1,251 recovery ZIP entries were size/hash verified and matched the accepted source. ZIP SHA256: `7f44bf7c0b2dbac1ff940093842eccba5fd60718cef4a117c4887edbd54a43c1`.
- Source manifest SHA256: `a0ef6b885dd457f738dc086565630f3b839f7dd93adf04540fd0e6281e6041b7`.
- Fresh consistent PostgreSQL dump SHA256: `380db6a95d652ebf5dd2c8c7314a79fc3d7eaff93520e838dfdced016b7127c6`. An isolated restore matched all 84 recorded CPL table digests; all 68 evidence files were copied and verified.

The first commit attempt failed because the isolated verification environment lacked Git author identity. It was retained and corrected using command-local existing owner identity settings; no global configuration or hook bypass was used. The successful commit left a clean source tree before Phase 6 edits.

The private `PHASE5_CHECKPOINT_COMPLETE.json` binds these records; SHA256 `c9ab7b5cbceaf19b2e4555fe5d82023bc8857539063d519290713c7b8d45c431`. Private evidence is not included in public source.

## Implementation boundary

Company settings now include connection state, selected source, granted scopes, last/next sync, mapping versions and source receipts. Anonymous forms and signed backend submissions share durable intake. The actual Google OAuth/Gmail adapter runs through an isolated HTTP fixture in local DEVELOPMENT. Ordinary background jobs produce non-ready leads for existing human review and configured proposal handoff.

Security boundaries include tenant RLS, current session/membership/module permission, CSRF, source-read authorization, immutable originals, generation and lease fencing, per-connection refresh serialization, authenticated encryption, exact-body signature/idempotency checks, bounded persistence and safe original downloads. See [implementation details](PHASE6_INTAKE_IMPLEMENTATION.md).

## Original Phase 6 checkpoint: migration and preservation

Frozen additive migrations:

- `0038_cpl_company_integrations.sql`: SHA256 `32ca02601ebe5d3712a5a3fd7b95795de45430e4ecefbea4bdf18aecaa3dc6f0`.
- `0039_cpl_durable_inbound.sql`: SHA256 `06ebea3c21112f6b9700c6411b728f2dfcd2bfbbaeee9c84939cad540b6997d7`.

The isolated restored Phase 5 database upgraded successfully before the local owner database. Every one of the 1,084 original rows across the predeclared 75 protected business tables was preserved. Receipt `isolated-migrations-1790216258913.json`, SHA256 `733c10e23f01d470a4ee8971d969f29ff77dade7d259d3a56e1de1bd2ab33089`.

The normal local launcher then applied the same migrations. Immediate post-migration comparison passed all 75 protected tables/1,084 original rows and all 68 original evidence files. Comparisons retain original columns and allow additive new records. Authentication sessions/identity fixtures, platform/auth audit and local metadata were explicitly excluded before mutation; the business baseline was not narrowed after a failure.

At the original Phase 6 checkpoint, the final comparison after its two-company workflows passed the same 75 tables, 1,084 original rows and 68 original files. New synthetic services, narrowly matched recipes, forms, mappings, connections, receipts, leads and unapproved drafts are additive. Accepted company policies/catalog items/recipes/templates and historical agreements were not replaced.

That original Phase 6 consistent checkpoint contains 98 CPL table digests and 68 byte-verified evidence files. Dump SHA256: `50ded2ad414c85d1b3aaa48a18069d855a3a1876faad7b02fd9bd1eabec5201a`; manifest SHA256: `5e95a09b5f257d8ad6d7b840ebfc7dbc4c580b723de24c0dea02c16cc8273cfd`. Its archive listing is verified; restoration of this final dump is **NOT RUN**, distinct from the earlier successful Phase 5 isolated restore/upgrade. ACLs are retained. The private recovery policy explains the unsafe default PUBLIC function privileges introduced by `pg_restore --no-acl`; restoration must verify reviewed grants before exposing runtime roles. Existing external DPAPI secrets remain separately protected and are not exported in plaintext.

## Original Phase 6 verification evidence (historical)

The following records belong to the original Phase 6 checkpoint. The session/browser dispositions are superseded by the stabilization section below; other historical passes are not silently rerun or added twice. These remain distinct evidence levels, not a combined live-provider claim:

- **250 real PostgreSQL tests PASS across 13 complete files:** hosted 34, administration 28, automation 26, commercial 25, execution 12, field 17, report 15, delivery 25, company 18, company schema 1, inbound 22, Gmail worker 23 and local entry 4. Restricted roles/RLS, retries, fencing and retained Phase 1–5 workflows are exercised. Consolidated backend receipt SHA256: `8d85c2161cc0db1eaa8394ac00bc9e872a1e00e59ee9e1e5236e592c76b35e69`.
- **175 unit tests PASS** in the final nine-file batch, including the actual provider/cryptography 61, HTTP/runtime/readiness and local identity/session boundary tests. Separately, **12 entry-policy**, **14 integration API** and **70 component** assertions passed. These are nonoverlapping groups; the earlier provider 61 and auth 20 are included in 175, not added again.
- **Two-company actual HTTP acceptance: 13 checks PASS.** Anonymous inquiry → durable receipt → ordinary worker → non-ready lead → explicit authorized owner API review → enabled exact-service recipe → unapproved draft passed for Alder and Harbor. Cross-company receipt/evidence/lead/proposal IDs were denied, scoped source lists remained isolated and earlier company configuration hashes matched. Receipt SHA256: `260b95ef2e8e53e5fd9defb10f289ce4754c9fb21b1e19dcad80c2888944c0e6`. Drafts are Alder `Q-2026-F8D2225B52DF` v1 USD and Harbor `Q-2026-E2956936715B` v1 CAD. This was API review, **not browser acceptance**.
- **Actual signed HTTP:** the documented sender, same-event retry, fresh-nonce deduplication, changed-body conflict and forged-signature denial ran against the app. The corrected same-record continuation passed 15 checks, including exact original evidence, anonymous/cross-company denial and revoked-source refusal. Receipt SHA256: `8eb17f708c93bc2c15da9ef3b00604822c81a552dc2d494c5fa0f65e9919207b`.
- **Actual Gmail LOCAL_FIXTURE HTTP: 16 checks PASS.** The real authorization-start/callback/identity/scope adapter, selected label, bounded one-day policy and ordinary worker created two receipts and two non-ready leads. The new connection was paused afterward. Receipt SHA256: `d3a35a006d309700182d5a6654dc391efb64809423a901e8c87e077484cd8147`. Gmail worker PostgreSQL tests are already included in 250. No real Google consent or mailbox was used.
- **Normal STOP/RUN and held-work restart PASS.** A single new unclaimed job was held until the normal restart, then released to the ordinary worker. Same source/configuration/encrypted credential hashes, one lead and one completed processing attempt were verified. Receipt SHA256: `d71842cd6a21677767d96af0f82d914aa2aa3eaa915ea1029f47790f4cb8e9ba`. No manual drain was used; no held job remains. Temporary signed-test sources were disabled and their keys revoked.
- **Historical session outcome: 52 PostgreSQL cases UNFINISHED / NOT ACCEPTED; 2 offline assertions PASS separately.** The isolated fixture timed out after setup and repeated statement timeouts. Its log records a 125-second filesystem checkpoint; this establishes substantial test-cluster I/O, not a proven application defect or deadlock. Final assertion results are unknown. The disposable cluster was verified stopped without changing the owner database. No blanket pass across 14 files or 302 tests was claimed at that checkpoint. All 52 original PostgreSQL case dispositions are now reconciled in the stabilization ledger below.
- **Historical browser outcome: required Phase 6 flow NOT RUN.** After the planned stop, the available in-app tabs retained connection-error pages. Browser URL policy blocked their refresh; the necessary manual reopen was requested. Phase 5 entry/switch browser proof remains valid historical evidence but does not satisfy the new form, mapping, fixture, narrow-screen or post-upgrade browser checks.

Earlier failed attempts remain alongside corrected logs. Actual route testing found an entry-policy omission that returned 503 for authorized original-evidence and saved-mapping-history reads; both now pass the narrow allowlist regression. Actual intake found date-only values rejected by the existing timestamp domain. Preview, processing and reprocessing now share validated conversion to midnight in the saved company timezone, while raw evidence remains unchanged. The original failed Alder receipt retained all five failures and recovered through an authorized normal retry, without duplicate lead or draft creation.

The package transition's missing export was resolved by normal workspace-copy synchronization. A subsequent cold development compile returned transient `Manifest file is empty` / JSON parsing errors on one mapping read and `/workspace`; later requests succeeded. The cause is not established. It is retained as an unresolved local development limitation, not attributed to Cloudflare or claimed fixed by longer test timeouts. Private restart-harness casing and state expectations were corrected separately without weakening production assertions. Tests do not establish physical phone/camera behavior, native save-dialog completion, hosted runtime or production identity acceptance.

## September 24 stabilization: corrections and measured results

Canonical repair baseline is `b03e3801c89bc64903266d8829e3ec08b6cc0081`. Its source recovery and 28 prior receipt hashes were checked before repair; a new consistent pre-repair dump and a broader predeclared 89-business-table/1,320-row baseline supplement the original 75-table/1,084-row/68-file preservation scope. They are different scopes, not additive totals. The final comparisons and their separate operational-counter disposition are recorded below. No earlier approved artifact was regenerated in place.

The narrowly changed areas are:

| Area                                                   | Correction                                                                                                                                                                     | Evidence and limits                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `scripts/cpl-local.mjs`                                | Sequential finite anonymous entry checks for workspace, session, local config and real SQL status; READY only after all contracts pass; Doctor/reuse checks current readiness. | 27 bootstrap cases PASS. No session is granted by readiness. Security, process ownership, compiler/cache settings and production isolation remain. Scoped lint timed out after 180 seconds. Complete normal-gate outcomes are recorded separately in the private release ledger; this checkpoint does not claim a strict-lint pass.                                                                                                          |
| `cpl-session-http-postgres.test.ts`                    | Replace over-broad cascading fixture TRUNCATE with FK-ordered transactional DELETE of six seeded tables.                                                                       | 52 original real-PG cases and 2 separate offline cases PASS. Measured cleanup exhausted 10-second hooks before case bodies; all original cases/assertions, intentional concurrency, role/RLS checks and existing deadlines remain. The owned PostgreSQL process was stopped and disposable test database dropped; archived failure-cluster directories are not claimed deleted. The owner database was not test input.                       |
| Integration receipt DTO/UI and focused component tests | Render nested normalized custom answers through explicit escaped scalar fields, current-policy labels and stable-ID fallback; preserve false/null distinctions.                | 15 integration UI + 13 public-form cases PASS; web source-alias/domain types PASS. Original bytes and date normalization are unchanged by this display fix. Native date interaction was exercised with ordinary keyboard; no date implementation rewrite.                                                                                                                                                                                    |
| `phase22-executive-pdf.test.ts`                        | Move unchanged demo setup/settings to beforeEach with a finite 60-second setup budget; retain the 30-second substantive callback and teardown/assertions.                      | Measurements showed approximately 10–11 seconds setup plus variable first PDF-library load. Corrected diagnostic runs passed at the original callback budget; ordinary installed 1/1 passed after separation. This is the legacy executive PDF persistence test, not a customer proposal artifact test or verification of every commercial PDF control. Complete canonical/public normal-gate outcomes are separate release-ledger evidence. |

The historical `Manifest file is empty`/JSON parsing error was not reproduced in the baseline or completed corrected-readiness cold cycles. Installed Next already separates `.next/dev` from production `.next`; no shared-output collision or single compiler root cause was established. The readiness repair closes a demonstrated coverage gap: actual anonymous session/config routes previously compiled after READY. Passing cycles do not erase the historical failure or prove its root cause fixed. Operational acceptance may close only after the prescribed startup, session, UI, worker-recovery and preservation checks pass; that conclusion remains separate from a proven compiler root cause.

| Startup condition                                                                                   | Current outcome                                                                                                                                                                                                                                                                     |
| --------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Cold 1: stopped processes, existing generated development output retained                           | PASS; four real readiness contracts then 10 two-company fresh-session/preserved-record API checks. Readiness-probe counter 100.6 seconds.                                                                                                                                           |
| Cold 2 (build-cold): stopped processes, only regenerable development output preserved in quarantine | PASS; 282 entries moved with names/sizes/timestamps verified, no deletion; 10 API checks PASS. Readiness-probe counter 131.7 seconds.                                                                                                                                               |
| Cold 3: normal development start after the production build, with generated outputs retained        | PASS; readiness-probe counter 75.8 seconds, then 10 API checks and both logouts. The first metadata-only readback preflight refusal is retained; correcting the condition enum did not relaunch or change the app.                                                                  |
| Warm 1: ordinary restart and fresh entry                                                            | PASS; 73.7-second readiness-probe counter, 10 API checks and both logouts. The separate continuity helper failed because its expectation contradicted existing per-launch signing-secret rotation; original failure retained.                                                       |
| Warm 2: ordinary restart with held new intake work                                                  | PASS; 89.5-second readiness-probe counter, 10 API checks and both fresh-session logouts. The same held job completed once through the ordinary worker after exact release; original evidence/configuration remained identical. The separately measured old-cookie rejection passed. |

The local development launcher intentionally creates a new signing secret each start. A previous-launch cookie therefore becomes invalid even before its declared expiry; sign in again through the normal development entry controls. This existing policy is covered by the prior-launch-secret unit test. It is not a claim that the SQL session row was revoked or naturally expired.

The warm 1 failure is preserved as an invalid helper expectation. Warm 2 uses a new probe that requires an exact anonymous local session response, protected-lead HTTP 401 and logout HTTP 401 with the same in-memory old cookie. It does not sign in again after restart; separate cycle checks verify fresh entry. The corrected warm 2 probe passed all three expected responses before the original declared expiry.

All five cycles used the same frozen source snapshot. Their 50 passing observations are five repetitions of ten assertions, not 50 distinct tests. Warm 2’s held-work probe temporarily adjusted only one new unclaimed job’s available_at, restored that exact field, and verified one ordinary-worker completion, one linked review lead, immutable original/configuration/key hashes and cleanup of only the temporary source/key. No manual drain was used.

The counters above exclude initial toolchain/schema work before the web child; they are not total startup SLAs. Actual root-recorded condition/log/Doctor evidence is bound separately. Two private cycle-controller attempts failed before app launch; a harmless command differential established native executable-path separator handling as that helper's cause. The initial quoting hypothesis was disproved. Unchanged normal CMD launchers succeeded; those harness failures are not application cold cycles.

All 67 selected restricted-PG cases passed (inbound 22, Gmail 23, company 18, local entry 4). With the 52 session cases, 119 are fresh real-PG; 183 other cases retain historical logs and recorded test/source parity. The union is 302 distinct real-PG across both checkpoints, plus 2 offline, not 302 fresh or 369. The 27 bootstrap, 28 components and one PDF case remain separate evidence groups. Repeated diagnostic/canonical/public PDF executions and overlapping historical component totals are not additional tests. Each recorded result binds its own source/configuration scope; byte parity is not a new execution.

All 14 pages from seven complete new legacy executive PDF persistence-test artifacts were rendered and visually inspected with no layout finding. A separate addendum inspected both pages of the failed diagnostic’s retained temporary PDF. That unfinalized file is not a successful persistence result, and this does not claim future gate PDF coverage. The unchanged legacy synthetic fixtures say “Generated using live OpenAI”; that wording does not establish live provider use. The first diagnostic failure had 5.11 seconds of journal overhead and remains retained; corrected instrumentation had about 18 milliseconds of overhead. Instrumented passes do not substitute for ordinary/full-gate results.

### Actual browser and HTTP corroboration

The owner manually reopened the designated in-app browser. The agent then performed real application UI interactions, using the authorized synthetic identities, for both records:

| Company | New browser inquiry                                            | Receipt                      | Ordinary-recipe draft             |
| ------- | -------------------------------------------------------------- | ---------------------------- | --------------------------------- |
| Alder   | PHASE 6 STABILIZATION FICTIONAL - Alder browser inquiry 0924A  | INQ-C72255996F0C50042AE99B56 | Q-2026-7EE09991C8AD v1 USD, DRAFT |
| Harbor  | PHASE 6 STABILIZATION FICTIONAL - Harbor browser inquiry 0924B | INQ-5A42F9C534EF04F6E3CE8308 | Q-2026-A498FFDE52B1 v1 CAD, DRAFT |

Observed actions include required validation, actual public submission, original/mapping/normalization views, reasoned reprocess, human correction/save/reload/reasoned ready decision, ordinary-worker draft linkage, replayed recipe result, unsaved Keep editing, company/role isolation, and a measured 390-pixel public-form/keyboard check. Alder's original access=false remains immutable; the deliberate human save confirmed true. Harbor's date-only answers 2026-10-16/13 remain original while saved normalization uses America/Los_Angeles at 07:00Z. Neither draft was approved, awarded or sent.

Separate authenticated HTTP evidence passed 9 aggregate checks including 8 reciprocal access denials, same original bytes/event returning the same receipt twice per company, changed-body HTTP 409, one linked lead/draft and exact retained human corrections. It corroborates saved records; it is not a substitute for the preceding UI actions. At this September 24 repair checkpoint, the final post-cycle visible browser reopen is **BLOCKED / LOCAL OWNER ACTION REQUIRED**. A retained connection-error page refused normal navigation under the browser URL policy; a manual reopen was requested. The earlier agent-operated real UI workflows passed, and all five restart HTTP readbacks passed, but neither substitutes for this outstanding browser check. Native download-save dialog and physical phone/camera acceptance remain NOT RUN. The earlier browser-policy error-page block is retained as historical tool evidence, not an application/DB diagnosis.

### Preservation and release boundary at the dated checkpoint

Final comparison of the original 75 protected tables / 1,084 rows and 68 evidence files passed. The broader pre-repair 89-table / 1,320-row comparator remains **FAIL**: 1,317 original rows are byte-identical, no original keys are missing, and three existing inbound rate buckets changed only count/expiry fields. The recorded two-form retries/conflicts and later held inquiry explain those changes under the unchanged rate-admission function. The baseline and exclusions were not narrowed or recaptured, and no business record was restored or overwritten to make a comparison pass. This operational explanation does not turn the full comparator into PASS.

A new consistent recovery checkpoint contains 98 table digests and 68 verified evidence files. The 10,725,531-byte dump has SHA256 `9eccf3bd75159a0257801d67e35eab1b138b3eee87aa0cf1f8b617917fc5e51f`; its manifest SHA256 is `0fded9cc588ee52e8da128fd116d211ab539fa53f0a6169edae34521cd51cd3e`. Exported-snapshot consistency and the archive structure listing passed. Restoration of this new dump is **NOT RUN**, distinct from earlier isolated restore evidence.

Seven installed-package typechecks and all 14 production Turbo tasks passed, with zero cached tasks and a Turbo-reported duration of 10m5.214s. The tested 1,295-file snapshot remained unchanged throughout all five cycles; documentation-only checkpoint edits are subsequent source changes. The original scoped lint timeout remains preserved rather than relabeled PASS.

This dated source document records the verified repair checkpoint only. The separate authoritative private stabilization release ledger records measured post-cycle browser outcomes, normal canonical/public gate results, exact commits, recovery archives, source publication/readback and final operating state. Those outcomes are not claimed by this document or promised for a future release.

## Open and test

Use the normal `RUN-CPL-COMMAND-CENTER.cmd` and <http://127.0.0.1:3400/workspace>. Enter as synthetic owner Alpha for Alder or owner Beta for Harbor. Navigate **Company → Integrations & intake**. Follow [the local testing guide](LOCAL_PHASE6_INTAKE_TESTING.md) for form publication, source review, mapping history and the explicitly synthetic Gmail path.

The source package contains no acceptance database, credentials, customer data or generated evidence. A sanitized public checkout cannot reuse the canonical runtime ownership marker or local database.

## Publication and deployment

Source publication is independent of local product acceptance. The original Phase 6 canonical checkpoint is `b03e3801c89bc64903266d8829e3ec08b6cc0081`; the original public baseline was `cd9184c6075854b30b1d6dca00a609ca5f085945`. The stabilization release receipt records the measured canonical commit/recovery, exact reviewed public candidate, normal gate outcome, any authorized forward push and unauthenticated readback. These documents make no claim that a future publication succeeds.

Bounded read-only review observed both known Workers' Git connection controls unconnected and the complete 12-application account list without the target public repository association; the public repository's Actions/hooks/active-job/deployment-record boundary was separately checked. This is an observation within that scope, not universal proof about every external trigger or account. No automation setting was changed. Fresh candidate-specific privacy/gate/linkage checks, commit/push outcomes and anonymous readback are separate private release-ledger evidence. This document makes no publication or deployment claim.

Deployment: **NOT PERFORMED**. No cloud settings/resources/schedules changed, new real-account grants, real mailbox reads, customer messages or paid product-AI calls occurred in this repair. Existing provider access was read-only for the authorized publication review. Local fixture credentials and records remain synthetic and private.

## Remaining tracks

Complete only the remaining evidence-bound local acceptance and source checkpoint for 6A–6B; do not begin another product phase as a substitute. Real Gmail acceptance requires separate scoped owner authorization, secure real identity, dedicated client/callback and external encryption configuration, approved test mailbox/selection, consent/scopes and Google verification/security requirements. Use [the live checklist](PHASE6_LIVE_GMAIL_ACCEPTANCE.md); do not execute it implicitly.

Phase 6C is external sending, calendar and CRM synchronization with governed, verified provider actions. Phase 7 knowledge/AI does not replace this unfinished integration scope. Hosting/Hyperdrive/domain/capacity optimization remains **DEFERRED**.
