# Local Phase 6 intake testing

**Verified repair checkpoint: September 24, 2026. Phase 6A–6B is IMPLEMENTED; full local acceptance is NOT COMPLETE at this checkpoint.** The original two-company HTTP workflows remain verified. New Alder and Harbor inquiries also passed through agent-operated real public-form UI, source review, explicit human-ready decisions and ordinary enabled-recipe draft creation. Separate HTTP checks verified exact retries/conflicts, one lead/draft and cross-company denials. Component/API evidence remains distinct from browser evidence.

**Final post-cycle visible readback: BLOCKED / LOCAL OWNER ACTION REQUIRED at this checkpoint.** The owner manually reopened the designated browser for the earlier walkthrough; the agent performed the UI interactions. After five completed normal startup/restart cycles, a retained error page again refused normal navigation and the owner was asked to reopen the workspace. This dated source document records the verified repair checkpoint only. The separate authoritative private stabilization release ledger records measured post-cycle browser outcomes, normal canonical/public gate results, exact commits, recovery archives, source publication/readback and final operating state. Those outcomes are not claimed by this document or promised for a future release. A browser error page does not establish an application or database failure. Native download-save dialog and physical device checks remain separately NOT RUN. See [PHASE6_HANDOFF.md](PHASE6_HANDOFF.md).

Real Gmail consent and mailbox access are **DEFERRED**. A screen marked **LOCAL FIXTURE** uses controlled synthetic provider responses. It does not access a real mailbox. Hosting, public website connection and external sending are outside these local steps. See [the separate live Gmail checklist](PHASE6_LIVE_GMAIL_ACCEPTANCE.md).

## Open the existing companies

Use the normal `RUN-CPL-COMMAND-CENTER.cmd` launcher and open <http://127.0.0.1:3400/workspace>. Wait for the launcher’s application READY result after workspace, anonymous session/config and SQL checks; Next.js’s earlier “Ready” compiler line alone is insufficient. Doctor reports current readiness. Choose **Development identity → Enter development workspace**, or **Switch development identity** when already signed in. These are synthetic identities without real provider authentication or MFA.

Use **Synthetic company owner Alpha → PHASE 5 - Alder Inspection Services · Synthetic** and **Synthetic company owner Beta → PHASE 5 - Harbor Design Studio · Synthetic** separately. Keep their existing companies, memberships, current intake policies and all approved work intact. Do not replace either company or simplify its policy to make ingestion pass. The shared synthetic manager has different roles in Alder and Harbor; check the displayed company and role before each action.

The local development launcher intentionally creates a new signing secret each start. A previous-launch cookie therefore becomes invalid even before its declared expiry; sign in again through the normal development entry controls. This existing policy is covered by the prior-launch-secret unit test. It is not a claim that the SQL session row was revoked or naturally expired.

All five normal cycles passed their fresh-session record readback. Warm 2 also verified expected old-cookie rejection and one held job completed once through the ordinary worker. The warm 1 continuity probe’s failed assumption remains documented. Final visible record reopening is BLOCKED at this dated checkpoint; the separate private stabilization release ledger records any later measured outcome.

If entry fails, retain the displayed safe reference and retry through the normal launcher/entry controls after the reported prerequisite is corrected. Never paste sessions, cookies, database configuration, source credentials or callback URLs into an issue. Readiness is a prerequisite check; successful sign-in must still issue and read a real local session.

## Inspect the examples exercised through the UI

These are additional fictional records, separate from the older HTTP examples below. They reuse each company's already enabled Phase 6 form/service/mapping/recipe; earlier accepted records and configuration were not replaced.

| Saved record       | Alder                                                             | Harbor                                                                   |
| ------------------ | ----------------------------------------------------------------- | ------------------------------------------------------------------------ |
| Inquiry/lead title | **PHASE 6 STABILIZATION FICTIONAL - Alder browser inquiry 0924A** | **PHASE 6 STABILIZATION FICTIONAL - Harbor browser inquiry 0924B**       |
| Receipt            | `INQ-C72255996F0C50042AE99B56`                                    | `INQ-5A42F9C534EF04F6E3CE8308`                                           |
| Proposal           | **Q-2026-7EE09991C8AD**, v1, DRAFT, USD                           | **Q-2026-A498FFDE52B1**, v1, DRAFT, CAD                                  |
| Saved review       | Lead v3; human-confirmed access=true, source remains false        | Lead v3; **Concept study** preserved                                     |
| Requested dates    | Original dates blank                                              | Visit 2026-10-13; deadline 2026-10-16, normalized in America/Los_Angeles |

1. Enter as the matching synthetic owner, select the matching company, and open **Company settings → Integrations & intake → Source receipts**. Find the exact reference. Inspect the saved original, mapping v1, processing history, normalized values and linked lead without resubmitting the inquiry.
2. Normalized custom answers show individual escaped values. **No** preserves a submitted false value; **Not supplied** denotes null. A current-policy label is labeled as current, and a stable field ID remains available when no current label exists. The displayed original does not change after a human correction to the linked lead.
3. Open the linked lead and saved draft/execution. Both ready decisions were explicit human actions; the normal enabled recipe prepared one draft. These are unapproved examples, not awarded projects, approved PDFs, customer deliveries or invoices.
4. For final restart readback, inspect the same reference/version after normal READY. This final visible reopen was BLOCKED at the dated checkpoint; retain its separate result in the private stabilization release ledger. Do not create replacement records simply to obtain a clean screen.

The visible walkthrough exercised required-field feedback, saved mapping and source views, a reasoned reprocess, Keep editing through an attempted identity switch, exact ready-review reload/reason, automatic draft linkage and company switching. The shared synthetic manager saw Directory-only company settings as a Harbor member and read-only integration configuration as an Alder manager. Switching back to Harbor cleared Alder source details. A 390-pixel public-form keyboard check passed; this is not a physical phone test. Ordinary keyboard date entry and Tab committed Harbor's dates; a tool fill that had not committed native control state was not accepted as saved input.

## Inspect the saved HTTP acceptance examples

These visibly synthetic records already exist. The shared suffix `817bc55e` identifies this acceptance run. Inspect them before creating another example; do not resubmit their inquiries merely to open the results.

| Saved record           | Alder                                                                    | Harbor                                                                   |
| ---------------------- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------ |
| Published form         | **PHASE 6 FICTIONAL - Alder HTTP inquiry 817bc55e**                      | **PHASE 6 FICTIONAL - Harbor HTTP inquiry 817bc55e**                     |
| New service            | **PHASE 6 FICTIONAL - Alder HTTP acceptance service 817bc55e**           | **PHASE 6 FICTIONAL - Harbor HTTP acceptance service 817bc55e**          |
| Saved mapping          | **PHASE 6 FICTIONAL Alder intake mapping 817bc55e**, version 1           | **PHASE 6 FICTIONAL Harbor intake mapping 817bc55e**, version 1          |
| Enabled recipe         | **PHASE 6 FICTIONAL - Alder reviewed intake draft 817bc55e**, version 1  | **PHASE 6 FICTIONAL - Harbor reviewed intake draft 817bc55e**, version 1 |
| Linked lead            | **PHASE 6 FICTIONAL - Alder anonymous service inquiry 817bc55e**         | **PHASE 6 FICTIONAL - Harbor anonymous service inquiry 817bc55e**        |
| Receipt                | `INQ-88CC2880533C254631004B5F`                                           | `INQ-9013DD3AB8FC7D3F1D0C574A`                                           |
| Resulting proposal     | **Q-2026-F8D2225B52DF**, version 1, **DRAFT**, USD                       | **Q-2026-E2956936715B**, version 1, **DRAFT**, CAD                       |
| Intake policy reviewed | Version 2, including access confirmation and **Access review reference** | Version 1, including the saved **Design brief** choice                   |

1. Enter as the corresponding owner and select the correct company. Open **Company settings → Integrations & intake → Inquiry forms**, then the saved form above. Both are enabled at configuration version 2 and publish only their own new synthetic service. Use **Preview saved customer form** or **Open saved public inquiry form** to inspect the customer view without submitting it.
2. Open **Source receipts** and locate the matching reference. Inspect the saved original, mapping version, normalization and linked lead. Both inquiries first produced non-ready leads; the acceptance utility then performed the explicit authorized owner review through the normal APIs. The new lead-ready recipes, each restricted to its own new service, produced the drafts through the ordinary worker.
3. Use **Open linked lead for human review** to inspect the saved answers. Both requested visit dates were **2026-10-06** and requested deadline dates **2026-10-10**. Normalization records midnight in the saved company timezone: **America/New_York** for Alder and **America/Los_Angeles** for Harbor. These are requested dates, not scheduled visits; original date-only answers remain unchanged.
4. Open **Company settings → Configure workflow recipes in Action Center** to inspect the exact new recipe and its recorded execution. The existing Alder task-only recipe remains unchanged. Open the proposal reference above from the linked execution or proposal list. Neither draft is approved, awarded, converted to a project, exported as a PDF or sent.

Alder's receipt retains five failed attempts from the discovered date-mapping defect, followed by a successful normal authorized retry after the correction. The same original byte count and hash were verified; no replacement inquiry or manual worker drain was used. Harbor processed on its first attempt. Earlier cold development-route and acceptance-utility failures remain recorded separately; they are not counted as successful checks.

## Create an additional fictional form when needed

Open **Company settings → Intake rules** to read the current saved questions first. Then open **Company settings → Integrations & intake → Inquiry forms**.

| Company | New browser example                                                          | Preserve these current requirements                                                                                                                                                                                                                                    |
| ------- | ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Alder   | Use a new **PHASE 6 FICTIONAL** name distinct from the saved HTTP example.   | Select the intended synthetic service deliberately. Retain site address, contact email, access confirmation and the required **Access review reference** text from current policy. Current catalog pricing is separate from the historical approved USD 650 agreement. |
| Harbor  | Use a different new **PHASE 6 FICTIONAL** name and fictional design request. | Select the intended Harbor synthetic service. Retain the required **Design brief** choice and its exact saved options, such as **Concept study** or **Layout review**. Do not copy Alder’s custom fields into Harbor.                                                  |

1. Enter **Internal form name**, **Customer form title** and **Customer introduction**. Use visibly fictional names and addresses; any example email should use a reserved `.invalid` domain.
2. Review **Customer questions**. Use **Add customer question** for each needed answer, select its supported **Intake destination**, and set the appropriate answer type, label, required state, length or exact choice options. Select existing company custom fields; do not invent replacements for their stable identities. Only deliberately selected questions are public.
3. Under **Published services**, use **Find an active service → Search services**, then select the intended company service. Selection persists across service pages. Published choices contain service descriptions, not internal prices or workflow settings.
4. Choose **Create matching form mapping** after the questions and destinations are correct. This saves a mapping version and selects it; the form still needs its own save. Alternatively, choose an existing matching **Mapping version**. Changing questions requires an explicitly matching mapping.
5. Enter **Receipt confirmation text**, then **Save form configuration**. Saving does not publish or submit the form. Verify the saved configuration version and retained values.
6. Choose **Preview saved customer form**. It shows the sanitized saved customer fields and services even while the form is disabled. It does not submit an inquiry or display internal company settings.
7. Enter a fictional acceptance reason in **Lifecycle reason**, then choose **Publish form**. Open **Open saved public inquiry form**. A later disable uses the same reasoned control; it does not erase historical receipts.

For mapping changes, open **Mappings**, select the intended saved mapping or create a new one, review **Source type**, supported **Destination** and **Text normalization**, and choose **Save mapping version**. **Preview mapping** uses an explicitly fictional sample and creates no receipt, lead or provider call. Original source claims are retained separately from normalized values; normalization is not inference or approval.

Use **View saved mapping version** to inspect an earlier immutable version without changing the current editor. A source receipt also offers its normalization's exact saved mapping. Action Center source attention opens the corresponding company-scoped connection or receipt.

## Submit and review an anonymous inquiry

1. Open the saved public form link in a separate signed-out browser context on the same local application. No employee sign-in is required. The page displays **DEVELOPMENT · Synthetic local inquiry** during loading, errors and receipt confirmation. This is a local application form, not a deployed company website or embedded widget.
2. Enter the fictional inquiry and choose the published service. Check required fields and date/choice controls. Choose **Submit inquiry** once and retain its receipt reference.
3. A transport failure leaves answers visible. Retry the same form attempt; it retains the event identity so an uncertain response does not intentionally create another inquiry. If the form configuration changed or the source conflicts, follow the displayed message instead of repeatedly creating fresh submissions.
4. As the corresponding owner, open **Company settings → Integrations & intake → Source receipts**. Use **Processing state**, **Refresh list** and page controls to locate the receipt. The ordinary local worker processes queued work; no manual queue drain is part of this path.
5. Open the receipt and compare original escaped source claims, normalized values, mapping version, processing attempts and the linked lead. **Download original evidence**, when permitted, checks the saved byte count and hash before offering an inert file; it does not open HTML. HTML, remote images and attachment bytes are not rendered or ingested. Attachment metadata is explicitly limited. Saving the file through a native browser dialog remains separate acceptance.
6. Choose **Open linked lead for human review** when available. Complete the normal company-specific intake requirements, supported directory/catalog selection, assignment, next action and duplicate review. Incoming sender claims and public answers never mark the lead ready automatically.
7. Save and deliberately review the lead under the current policy. Only an authorized **Ready for proposal** transition may trigger an enabled workflow recipe. The two saved Phase 6 services above already have matching draft-creation recipes; a new inquiry using one of those services can create another draft after this deliberate review. Preserve Alder’s accepted task-only recipe and all historical versions. If an additional service is needed, create a separate fictional service and an explicitly matching recipe rather than broadening an existing recipe. Without an enabled matching recipe, use the assigned action and deliberate **Create proposal** when eligible; report that as a manual path, not automatic handoff evidence.

Repeat with Harbor’s separate form and answers. Alder must not list Harbor receipts, original evidence or leads, and Harbor must not inherit Alder’s mapping, service or custom-field selections. Company/persona switches with unfinished work must show **Keep your work**; **Keep editing** retains the current context. Saved navigation must not produce a false unsaved-work prompt.

## Exercise the controlled Gmail fixture

Only proceed when the integration screen explicitly reports the controlled local fixture environment. Leave real Gmail authorization deferred.

1. Open **Mappings**, choose a new **Gmail message** mapping and explicitly map the supported subject, sender-name claim, sender-email claim and plain-text fields. Save the mapping version. Sender identity remains a source claim, not a verified customer identity.
2. Open **Gmail connections → New Gmail connection**, enter a fictional name, select the saved mapping and **Save connection configuration**.
3. Choose **Authorize controlled local fixture**. The callback returns to the workspace. Reopen the same company’s integration panel and saved connection; check its account, granted scopes and state. Authorization alone is not a successful sync.
4. Load the available label choices using **Load authorized label choices**, deliberately select the intended fixture label and bounded start policy, then save the connection configuration. Review cadence and any bounded backfill limit; do not select unrestricted mailbox history.
5. Enter **Operation reason → Queue sync now**. Check last attempt, last success, coverage and resulting receipts. The action queues durable work for the normal runner rather than processing it inside the button request.
6. Review resulting source receipts and non-ready leads through the same human-review flow. With a reason, **Pause intake**, **Resume intake** or **Disconnect local grant** exercises the saved lifecycle. Disconnect removes this application’s local grant and fences work; it does not claim provider-wide revocation.

The selected label limits this application’s ingestion, while Gmail’s read-only OAuth scope is broader. This implementation does not send messages, mark them read, change labels, fetch attachment bytes or run AI extraction. Fixture results do not establish Google consent, offline refresh or real mailbox acceptance.

## Signed sources, recovery and preservation

The saved form exposes a separate **Rotate signed source credential** action with a required reason. The returned secret is shown once for secure server-side custody and is excluded from form URLs and ordinary history. Use **I have stored it; hide credential** when finished. Never put it in a browser query, public form, screenshot or shared report. A signed sender must use the documented server-to-server request contract; rotating a credential alone does not send a test submission. **Revoke signed source credential** disables that local credential without deleting receipts.

Receipt recovery controls are permission- and state-specific. Enter a **Recovery reason**, then use **Queue retry** for an eligible failed attempt or **Explicit mapping version → Reprocess with selected mapping** for a deliberate new normalization. Original evidence and prior normalization remain. Reprocessing does not overwrite a human-edited lead or silently approve it.

Save forms before a normal Stop/Restart. After application READY, verify the same integration/form versions, receipts, linked leads and queued-work outcomes through normal entry. Preserve older Riverside, Cedar, Alder and Harbor approved PDFs, awards, delivery history and configuration snapshots. The original two-company HTTP and held-work results are historical passes; all three current cold starts passed fresh-session two-company checks; cold 3 followed the successful production build with outputs retained. Both ordinary warm restarts also passed fresh-session readback, and the held job completed once through the normal worker. An existing-cookie probe correctly verified rejection after per-launch signing-secret rotation; it did not test SQL revocation or natural expiry. Final visible browser reopening was BLOCKED at this dated checkpoint and is not waived by HTTP evidence. Required validation, visible source/reprocess/human-review/draft paths, role/company switching and the narrow keyboard interaction are already observed in the new browser walkthrough. Native download-save and physical device tests remain NOT RUN. Signed-source and Gmail fixture evidence is separate; no live provider or sending is implied.
